# Fermipy-based analysis to search for DM signatures from elliptical galaxies (EGs)

## For target selection, see `overlap/` directory 

## For individual target fitting, stacking, and plotting, see `run/` directory